module Projections
  class ReleasesTicketsProjection
    # def initialize(versions)
    #   @tickets_table = Repositories::TicketRepository.new.tickets_for_versions(versions)
    # end
    #
    # def ticket_for(jira_key)
    #   @tickets_table.detect { |ticket| ticket.key == jira_key }
    # end
  end
end
