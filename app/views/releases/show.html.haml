%h1 Releases for #{@app_name}

%table.table.table-striped
  %tr
    %th id
    %th date
    %th message
  - @releases.each do |release|
    %tr.release
      %td= release.id
      %td= release.time
      %td= release.subject_line

