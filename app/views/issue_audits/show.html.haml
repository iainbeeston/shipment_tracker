%h1= "Issue Audit for #{params[:id]}"

%h3 Ticket
%table.table.table-striped
  %thead
    %tr
      %th key
      %th summary
      %th status
      %th approver email
      %th approved at
  %tbody
    - if @ticket
      %tr.ticket
        %td= @ticket.key
        %td= @ticket.summary
        %td= @ticket.status
        %td= @ticket.approver_email
        %td= @ticket.approved_at.try(:utc)

- @reports.each do |report|
  %div{class: report.app_name}
    = render partial: 'issue_audit', locals: { report: report }
