= form_for(@token) do |f|
  = f.label :source
  = f.text_field :source
  = f.submit 'Create Token'

- table headers: ["Source", "URL"], classes: 'tokens' do
  - @tokens.each do |token|
    %tr
      %td= token.source
      %td= events_url(type: token.source, token: token.value)
